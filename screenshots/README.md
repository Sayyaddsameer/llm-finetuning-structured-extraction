# Screenshots Placeholder

> IMPORTANT: The `screenshots/` directory contains placeholder files.
> When running the actual LlamaFactory fine-tuning:
> 1. Take a screenshot of the full configuration panel BEFORE clicking Train and save as `training_config.png`
> 2. Take a screenshot of the loss curve at the end of training and save as `loss_curve.png`

See `training_config.md` for the exact configuration values used.
